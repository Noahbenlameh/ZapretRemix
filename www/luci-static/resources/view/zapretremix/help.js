'use strict';
'require baseclass';
'require view';

function section(title, body) {
	return E('div', { 'style': 'margin-bottom:24px;' }, [
		E('h3', {}, title),
		body
	]);
}

function p(text) {
	return E('p', { 'style': 'line-height:1.6;' }, text);
}

return view.extend({
	render: function () {
		return E('div', { 'class': 'cbi-map' }, [
			E('h2', {}, 'ZapretRemix — Инструкция'),

			section('Что это такое', p(
				'ZapretRemix — надстройка над уже установленным zapret2, чтобы управлять обходом блокировок ' +
				'провайдера через понятный интерфейс, а не вручную через SSH/uci. Ничего из zapret2 не заменяет — ' +
				'работает с тем же конфигом и теми же скриптами.')),

			section('С чего начать', E('div', {}, [
				p('1. На вкладке «Дашборд» посмотри, работает ли демон, и в каком режиме фильтрации он сейчас (справа от выпадающего списка).'),
				p('2. Если конкретный сайт/приложение не открывается — сначала попробуй «Тест и анализ»: введи домен, получи диагноз (доступен ли он вообды без обхода, или реально режется провайдером).'),
				p('3. Если домен приложения неизвестен — используй «Поиск по приложению»: начни захват, попользуйся приложением на телефоне, останови захват и добавь найденные домены в «Ресурсы».'),
				p('4. Подбери стратегию на вкладке «Стратегии» — начни с «По умолчанию», если не помогает — пробуй другие пресеты по очереди.')
			])),

			section('Режим фильтрации (Дашборд)', E('div', {}, [
				p('«Выключено» — стратегия применяется вообще ко всему трафику в доме, без разбора домена. Самый широкий охват, но и самая высокая нагрузка на роутер — используй только для диагностики или временно.'),
				p('«Только выбранные сайты» — стратегия применяется только к доменам из вкладки «Ресурсы». Предсказуемо, легко для роутера, но нужно вручную добавлять сайты.'),
				p('«Умный автоматический режим» — демон сам следит за признаками вмешательства DPI (повторные передачи, обрывы) и сам решает, каким хостам нужна помощь. Хороший баланс по умолчанию для повседневного использования.')
			])),

			section('Глубина обработки пакетов (Дашборд)', p(
				'Сколько пакетов каждого нового соединения обрабатывает демон. «Первые пакеты» хватает для большинства сайтов (там всё решается в момент установки соединения). ' +
				'«Всегда» нужно, только если проблема возникает не сразу, а глубоко внутри долгой сессии (например, приложение обрывается через какое-то время использования, а не сразу) — ' +
				'но это ощутимо дороже по нагрузке, особенно в режиме «Выключено», где это распространяется на весь трафик дома.')),

			section('Безопасность (таймер отката)', p(
				'При нажатии «Применить» (на Дашборде и в Стратегиях) настройка сразу вступает в силу, но появляется таймер на 20 секунд — если ничего не нажать, ' +
				'изменения автоматически откатятся к тому, что было до этого. Это защита от случая, когда новая настройка ломает интернет всему дому — тогда просто ' +
				'подожди 20 секунд, всё вернётся само.')),

			section('Тест и анализ — важная оговорка', p(
				'Это БЫСТРАЯ проверка (одна попытка, только TLS 1.2, без глубокого перебора стратегий) — она останавливает zapret2 на время теста и показывает, ' +
				'доступен ли домен вообще, без обхода. Это не полноценный blockcheck2.sh (тот перебирает десятки стратегий и может идти 10-30+ минут) — для действительно ' +
				'глубокого анализа сложных случаев запускай `/opt/zapret2/blockcheck2.sh <домен>` напрямую по SSH.')),

			section('Если что-то пошло не так', E('div', {}, [
				p('Полностью выключить: Дашборд → «Стоп». Интернет у всех в доме продолжит работать как обычно, просто без обхода блокировок.'),
				p('Если роутер стал недоступен/интернет пропал — подожди 20 секунд (если это было применение через ZapretRemix, сработает автооткат), либо перезагрузи роутер физически — zapret2 запустится с последним сохранённым (не временным) конфигом.')
			]))
		]);
	}
});
